{
  "scripts": {
    "prestart": "npm start /app",
    "start": "node server_app/server.js",
    "heroku-postbuild": "cd server_app && npm install && cd .. && cd app && npm install && npm run build"
  },
  "dependencies": {
    "concurrently": "^6.2.1"
  }
}
