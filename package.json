{
  "scripts": {
    "start": "node server_app/server.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install npm && npm run build && cd client_app && npm install && npm run build"
 },

  "dependencies": {
    "concurrently": "^6.2.1"
  }
}
