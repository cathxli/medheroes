{
  "scripts": {
    "start": "node server_app/server.js && cd app && npm start",
    "heroku-postbuild": "cd server_app && npm install && cd .. && cd app && npm install && npm run build"
  },
  "dependencies": {
    "concurrently": "^6.2.1"
  }
}
